# ==========================================
# Defense-Bot Frontend 環境變數設定檔 (範例)
# ==========================================
# 使用方式：cp .env.example .env，再依據實際環境修改各值

# [後端 API 位址]
# 用途：Nginx 反向代理 /api/ 時，會將請求轉送到此 URL（含 /api/v1/downloads/*）
# 必須填寫後端宿主機的真實 IP 或網域，不可使用 127.0.0.1
# （127.0.0.1 在容器內只指向 Nginx 容器自身，無法到達後端）
# 格式範例：
#   http://<BACKEND_HOST_OR_IP>:8088   ← 同區網本地 IP
#   https://<BACKEND_PUBLIC_DOMAIN_OR_IP>  ← 對外位址
BACKEND_URL=http://<BACKEND_HOST_OR_IP>:8088