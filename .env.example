# ==========================================
# Defense-Bot Frontend 環境變數設定檔 (範例)
# ==========================================
# 使用方式：cp .env.example .env，再依據實際環境修改各值

# [後端 API 位址]
# 用途：Nginx 反向代理 /api/ 和 /downloads/ 時，會將請求轉送到此 URL
# 必須填寫後端宿主機的真實 IP 或網域，不可使用 127.0.0.1
# （127.0.0.1 在容器內只指向 Nginx 容器自身，無法到達後端）
# 格式範例：
#   http://192.168.1.100:8088   ← 同區網的後端宿主機 IP
#   http://example.com:8088     ← 公開網域
BACKEND_URL=http://192.168.x.x:8088